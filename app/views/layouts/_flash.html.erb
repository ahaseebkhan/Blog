<% flash.each do |flash_type, message| %>
  <div class="<%= send "#{flash_type}_classes" %> border-t border-b px-4 py-3 text-center" role="alert">
    <p class="text-sm"><%= message %></p>
  </div>
<% end %>
